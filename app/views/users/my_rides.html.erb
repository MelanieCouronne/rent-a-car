<div class="container">
  <div class="row justify-content-center align-items-center">
    <p class="text-right link-to"><%= link_to "Back to home page", rides_path %></p>
    <div class="col-xs-12 col-sm-8 col-md-6">
      <h3 class="text-center mb-4 mt-3">Your rides</h3>
      <% @rides.each do |ride| %>
        <%= render 'shared/reminder_user_rides', ride: ride %>
      <% end %>
    </div>
  </div>
</div>
